import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'Effects',
	description: 'A stopwatch that runs a timer as a side effect and cleans it up when it stops.',
	concept: '$effect — side effects with cleanup',
	topic: 'Reactivity'
};
