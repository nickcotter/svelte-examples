import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'Derived values',
	description: 'Values that recompute automatically when the state they depend on changes.',
	concept: '$derived — computed reactive values',
	topic: 'Reactivity'
};
