import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'Component props',
	description: 'Pass data into a child component, with a default value for optional props.',
	concept: '$props — passing data into components',
	topic: 'Components'
};
