import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'If / else blocks',
	description: 'Show different content depending on the current value of some state.',
	concept: '{#if} / {:else if} / {:else} — conditional rendering',
	topic: 'Templating'
};
