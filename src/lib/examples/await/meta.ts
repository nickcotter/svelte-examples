import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'Await blocks',
	description: 'Render loading, success, and error states for a promise directly in markup.',
	concept: '{#await} — promise states in markup',
	topic: 'Templating'
};
