import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'Input binding',
	description: 'Keep a variable and a text input in sync, in both directions.',
	concept: 'bind:value — two-way binding on form inputs',
	topic: 'Bindings'
};
