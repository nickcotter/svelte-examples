import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'Transitions',
	description: 'Fade an element in and out as it enters and leaves the DOM.',
	concept: 'transition: — animating elements in and out',
	topic: 'Transitions'
};
