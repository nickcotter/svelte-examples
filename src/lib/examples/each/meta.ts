import type { ExampleMeta } from '$lib/registry';

export const meta: ExampleMeta = {
	title: 'Each blocks',
	description: 'Render a keyed list, and fall back to an else branch when it is empty.',
	concept: '{#each} — rendering lists',
	topic: 'Templating'
};
